---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="Redirecting…">
  <p>Redirecting…</p>

  <script>
    // OTP codes are now verified client-side on /welcome and /login-error.
    // This page only exists as a fallback for any old magic link emails.
    // Redirect to /login-error so the user can request a fresh OTP code.
    window.location.replace("/login-error");
  </script>
</Layout>
